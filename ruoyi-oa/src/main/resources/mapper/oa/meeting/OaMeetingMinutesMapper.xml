<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.oa.meeting.mapper.OaMeetingMinutesMapper">

    <resultMap type="com.ruoyi.oa.meeting.domain.OaMeetingMinutes" id="OaMeetingMinutesResult">
        <id property="id" column="id"/>
        <result property="meetingId" column="meeting_id"/>
        <result property="content" column="content"/>
        <result property="attachments" column="attachments"/>
        <result property="scope" column="scope"/>
        <result property="status" column="status"/>
        <result property="tenantId" column="tenant_id"/>
        <result property="createBy" column="create_by"/>
        <result property="createTime" column="create_time"/>
        <result property="updateBy" column="update_by"/>
        <result property="updateTime" column="update_time"/>
        <result property="remark" column="remark"/>
    </resultMap>

    <sql id="selectOaMeetingMinutesVo">
        select id, meeting_id, content, attachments, scope, status, tenant_id,
               create_by, create_time, update_by, update_time, remark
        from oa_meeting_minutes
    </sql>

    <select id="selectByMeetingId" parameterType="Long" resultMap="OaMeetingMinutesResult">
        <include refid="selectOaMeetingMinutesVo"/>
        where meeting_id = #{meetingId}
    </select>

    <select id="selectById" parameterType="Long" resultMap="OaMeetingMinutesResult">
        <include refid="selectOaMeetingMinutesVo"/>
        where id = #{id}
    </select>

    <insert id="insert" parameterType="com.ruoyi.oa.meeting.domain.OaMeetingMinutes" useGeneratedKeys="true" keyProperty="id">
        insert into oa_meeting_minutes
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="meetingId != null">meeting_id,</if>
            <if test="content != null and content != ''">content,</if>
            <if test="attachments != null and attachments != ''">attachments,</if>
            <if test="scope != null and scope != ''">scope,</if>
            <if test="status != null">status,</if>
            <if test="tenantId != null">tenant_id,</if>
            <if test="createBy != null and createBy != ''">create_by,</if>
            <if test="remark != null and remark != ''">remark,</if>
            create_time
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="meetingId != null">#{meetingId},</if>
            <if test="content != null and content != ''">#{content},</if>
            <if test="attachments != null and attachments != ''">#{attachments},</if>
            <if test="scope != null and scope != ''">#{scope},</if>
            <if test="status != null">#{status},</if>
            <if test="tenantId != null">#{tenantId},</if>
            <if test="createBy != null and createBy != ''">#{createBy},</if>
            <if test="remark != null and remark != ''">#{remark},</if>
            sysdate()
        </trim>
    </insert>

    <update id="update" parameterType="com.ruoyi.oa.meeting.domain.OaMeetingMinutes">
        update oa_meeting_minutes
        <set>
            <if test="content != null">content = #{content},</if>
            <if test="attachments != null">attachments = #{attachments},</if>
            <if test="scope != null and scope != ''">scope = #{scope},</if>
            <if test="status != null">status = #{status},</if>
            <if test="tenantId != null">tenant_id = #{tenantId},</if>
            <if test="updateBy != null and updateBy != ''">update_by = #{updateBy},</if>
            <if test="remark != null">remark = #{remark},</if>
            update_time = sysdate()
        </set>
        where id = #{id}
    </update>

    <delete id="deleteById" parameterType="Long">
        delete from oa_meeting_minutes where id = #{id}
    </delete>

</mapper>
