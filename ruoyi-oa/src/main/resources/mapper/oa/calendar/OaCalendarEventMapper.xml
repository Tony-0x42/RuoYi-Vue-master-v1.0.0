<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.oa.calendar.mapper.OaCalendarEventMapper">

    <resultMap type="OaCalendarEvent" id="OaCalendarEventResult">
        <result property="eventId"     column="event_id"     />
        <result property="title"       column="title"        />
        <result property="startTime"   column="start_time"   />
        <result property="endTime"     column="end_time"     />
        <result property="allDay"      column="all_day"      />
        <result property="location"    column="location"     />
        <result property="creator"     column="creator"      />
        <result property="type"        column="type"         />
        <result property="source"      column="source"       />
        <result property="sourceId"    column="source_id"    />
        <result property="status"      column="status"       />
        <result property="tenantId"    column="tenant_id"    />
        <result property="createBy"    column="create_by"    />
        <result property="createTime"  column="create_time"  />
        <result property="updateBy"    column="update_by"    />
        <result property="updateTime"  column="update_time"  />
        <result property="remark"      column="remark"       />
    </resultMap>

    <sql id="selectOaCalendarEventVo">
        select event_id, title, start_time, end_time, all_day, location, creator, type, source, source_id, status, tenant_id,
               create_by, create_time, update_by, update_time, remark
        from oa_calendar_event
    </sql>

    <select id="selectById" parameterType="Long" resultMap="OaCalendarEventResult">
        <include refid="selectOaCalendarEventVo"/>
        where event_id = #{eventId}
    </select>

    <select id="selectList" parameterType="OaCalendarEvent" resultMap="OaCalendarEventResult">
        <include refid="selectOaCalendarEventVo"/>
        <where>
            <if test="title != null and title != ''">
                AND title like concat('%', #{title}, '%')
            </if>
            <if test="type != null and type != ''">
                AND type = #{type}
            </if>
            <if test="status != null and status != ''">
                AND status = #{status}
            </if>
            <if test="creator != null">
                AND creator = #{creator}
            </if>
            <if test="source != null and source != ''">
                AND source = #{source}
            </if>
            <if test="params.beginTime != null and params.beginTime != ''">
                AND start_time &gt;= #{params.beginTime}
            </if>
            <if test="params.endTime != null and params.endTime != ''">
                AND end_time &lt;= #{params.endTime}
            </if>
        </where>
        order by start_time desc
    </select>

    <insert id="insert" parameterType="OaCalendarEvent" useGeneratedKeys="true" keyProperty="eventId">
        insert into oa_calendar_event (
            <if test="title != null and title != ''">title,</if>
            <if test="startTime != null">start_time,</if>
            <if test="endTime != null">end_time,</if>
            <if test="allDay != null">all_day,</if>
            <if test="location != null and location != ''">location,</if>
            <if test="creator != null">creator,</if>
            <if test="type != null and type != ''">type,</if>
            <if test="source != null and source != ''">source,</if>
            <if test="sourceId != null and sourceId != ''">source_id,</if>
            <if test="status != null and status != ''">status,</if>
            <if test="tenantId != null">tenant_id,</if>
            <if test="remark != null and remark != ''">remark,</if>
            <if test="createBy != null and createBy != ''">create_by,</if>
            create_time
        ) values (
            <if test="title != null and title != ''">#{title},</if>
            <if test="startTime != null">#{startTime},</if>
            <if test="endTime != null">#{endTime},</if>
            <if test="allDay != null">#{allDay},</if>
            <if test="location != null and location != ''">#{location},</if>
            <if test="creator != null">#{creator},</if>
            <if test="type != null and type != ''">#{type},</if>
            <if test="source != null and source != ''">#{source},</if>
            <if test="sourceId != null and sourceId != ''">#{sourceId},</if>
            <if test="status != null and status != ''">#{status},</if>
            <if test="tenantId != null">#{tenantId},</if>
            <if test="remark != null and remark != ''">#{remark},</if>
            <if test="createBy != null and createBy != ''">#{createBy},</if>
            sysdate()
        )
    </insert>

    <update id="update" parameterType="OaCalendarEvent">
        update oa_calendar_event
        <set>
            <if test="title != null and title != ''">title = #{title},</if>
            <if test="startTime != null">start_time = #{startTime},</if>
            <if test="endTime != null">end_time = #{endTime},</if>
            <if test="allDay != null">all_day = #{allDay},</if>
            <if test="location != null">location = #{location},</if>
            <if test="type != null and type != ''">type = #{type},</if>
            <if test="source != null and source != ''">source = #{source},</if>
            <if test="sourceId != null">source_id = #{sourceId},</if>
            <if test="status != null and status != ''">status = #{status},</if>
            <if test="tenantId != null">tenant_id = #{tenantId},</if>
            <if test="remark != null">remark = #{remark},</if>
            <if test="updateBy != null and updateBy != ''">update_by = #{updateBy},</if>
            update_time = sysdate()
        </set>
        where event_id = #{eventId}
    </update>

    <delete id="deleteById" parameterType="Long">
        delete from oa_calendar_event where event_id = #{eventId}
    </delete>

    <delete id="deleteByIds" parameterType="Long">
        delete from oa_calendar_event where event_id in
        <foreach item="eventId" collection="array" open="(" separator="," close=")">
            #{eventId}
        </foreach>
    </delete>

</mapper>
