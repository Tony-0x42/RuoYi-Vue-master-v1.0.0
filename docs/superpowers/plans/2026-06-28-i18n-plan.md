# 系统国际化（i18n）实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 为 RuoYi-Vue 前端引入可扩展的中/英文国际化能力，完成基础设施、公共文案和 `system/user` 示例模块改造，后端提供用户语言偏好支持。

**Architecture:** 前端使用 `vue-i18n@8` 按模块组织 locale 文件，Element UI 语言包联动；通过 `LangSelect` 组件切换语言并持久化到 store/localStorage；后端在用户表保存语言偏好并在登录时返回。

**Tech Stack:** Vue 2.6, Element UI 2.15, vue-i18n@8, Spring Boot, MyBatis

## Global Constraints

- 新语言只需新增 locale 文件并在 `src/i18n/locales/index.js` 注册，无需改业务代码。
- 所有翻译 key 采用 `模块.页面.元素` 三段式小写驼峰命名。
- 回退语言固定为 `zh-CN`。
- 树形表格页面（部门管理、菜单管理）本次不参与改造。
- 后端第一阶段只做用户语言偏好持久化和登录返回，不处理 controller/service 硬编码中文。
- 所有文本文件使用 UTF-8 编码。

---

## File Structure

### 新增文件

- `ruoyi-ui/package.json` — 添加 `vue-i18n@8` 依赖。
- `ruoyi-ui/src/i18n/index.js` — i18n 实例、语言切换辅助函数。
- `ruoyi-ui/src/i18n/locales/index.js` — 导出所有语言包。
- `ruoyi-ui/src/i18n/locales/zh-CN.js` — 中文语言包。
- `ruoyi-ui/src/i18n/locales/en-US.js` — 英文语言包。
- `ruoyi-ui/src/components/LangSelect/index.vue` — 语言切换组件。

### 修改文件

- `ruoyi-ui/src/main.js` — 引入 i18n 实例，联动 Element UI locale。
- `ruoyi-ui/src/store/modules/app.js` — 增加 `locale` 状态。
- `ruoyi-ui/src/store/getters.js` — 增加 `locale` getter。
- `ruoyi-ui/src/layout/components/Navbar.vue` — 插入 `LangSelect`。
- `ruoyi-ui/src/views/login.vue` — 替换登录页硬编码中文。
- `ruoyi-ui/src/utils/request.js` — 替换全局错误提示。
- `ruoyi-ui/src/plugins/modal.js` — 替换弹窗默认标题和按钮。
- `ruoyi-ui/src/utils/errorCode.js` — 替换错误码文案。
- `ruoyi-ui/src/utils/ruoyi.js` — 替换日期格式化中的中文星期。
- `ruoyi-ui/src/components/RightToolbar/index.vue` — 替换工具栏提示。
- `ruoyi-ui/src/router/index.js` — 路由 `meta.title` 改为 key。
- `ruoyi-ui/src/views/system/user/index.vue` — 用户管理页面改造。
- `ruoyi-ui/src/views/system/user/authRole.vue` — 分配角色页面改造。
- `ruoyi-system/src/main/resources/mapper/system/SysUserMapper.xml` — 用户表增加 lang 字段映射。
- `ruoyi-system/src/main/java/com/ruoyi/system/domain/SysUser.java` — 增加 `lang` 属性。
- `ruoyi-system/src/main/java/com/ruoyi/system/service/impl/SysUserServiceImpl.java` — 更新/注册时处理 lang。
- `ruoyi-admin/src/main/java/com/ruoyi/web/controller/system/SysProfileController.java` — 更新语言偏好接口。
- `ruoyi-framework/src/main/java/com/ruoyi/framework/web/service/SysLoginService.java` — 登录返回 lang。
- `ruoyi-admin/src/main/java/com/ruoyi/web/controller/system/SysLoginController.java` — getInfo 返回 lang。

---

## Task 1: Install vue-i18n and create i18n infrastructure

**Files:**
- Modify: `ruoyi-ui/package.json`
- Create: `ruoyi-ui/src/i18n/index.js`
- Create: `ruoyi-ui/src/i18n/locales/index.js`
- Create: `ruoyi-ui/src/i18n/locales/zh-CN.js`
- Create: `ruoyi-ui/src/i18n/locales/en-US.js`
- Modify: `ruoyi-ui/src/main.js`

**Interfaces:**
- Consumes: none
- Produces: default exported `i18n` instance; `setLocale(lang)` function; `messages` object with `zh-CN` and `en-US`

- [ ] **Step 1: Install dependency**

Run:
```bash
cd ruoyi-ui
npm install vue-i18n@8
```

Expected: `vue-i18n@8.x` added to `package.json` dependencies.

- [ ] **Step 2: Create locale files**

`ruoyi-ui/src/i18n/locales/zh-CN.js`:
```js
export default {
  // to be filled in Task 2
}
```

`ruoyi-ui/src/i18n/locales/en-US.js`:
```js
export default {
  // to be filled in Task 2
}
```

`ruoyi-ui/src/i18n/locales/index.js`:
```js
import zhCN from './zh-CN'
import enUS from './en-US'

export default {
  'zh-CN': zhCN,
  'en-US': enUS
}
```

- [ ] **Step 3: Create i18n instance**

`ruoyi-ui/src/i18n/index.js`:
```js
import Vue from 'vue'
import VueI18n from 'vue-i18n'
import messages from './locales'

Vue.use(VueI18n)

export const FALLBACK_LOCALE = 'zh-CN'
export const DEFAULT_LOCALE = 'zh-CN'

const savedLocale = localStorage.getItem('ruoyi-locale') || DEFAULT_LOCALE

const i18n = new VueI18n({
  locale: savedLocale,
  fallbackLocale: FALLBACK_LOCALE,
  silentFallbackWarn: true,
  silentTranslationWarn: true,
  messages
})

export function setLocale(lang) {
  if (!messages[lang]) {
    console.warn(`[i18n] locale ${lang} not found, fallback to ${FALLBACK_LOCALE}`)
    lang = FALLBACK_LOCALE
  }
  i18n.locale = lang
  localStorage.setItem('ruoyi-locale', lang)
  document.querySelector('html').setAttribute('lang', lang)
}

export default i18n
```

- [ ] **Step 4: Register i18n in main.js**

In `ruoyi-ui/src/main.js`, add near the top:
```js
import i18n from './i18n'
```

Change the Vue root instantiation from:
```js
new Vue({
  el: '#app',
  router,
  store,
  render: h => h(App)
})
```
to:
```js
new Vue({
  el: '#app',
  router,
  store,
  i18n,
  render: h => h(App)
})
```

Add Element UI locale linkage before `new Vue`:
```js
import ElementLocale from 'element-ui/lib/locale'
ElementLocale.i18n((key, value) => i18n.t(key, value))
```

- [ ] **Step 5: Verify build**

Run:
```bash
cd ruoyi-ui && npm run build:prod
```
Expected: build succeeds.

- [ ] **Step 6: Commit**

```bash
git add ruoyi-ui/package.json ruoyi-ui/src/i18n ruoyi-ui/src/main.js
git commit -m "feat(i18n): add vue-i18n infrastructure and locale registration"
```

---

## Task 2: Add common locale messages

**Files:**
- Modify: `ruoyi-ui/src/i18n/locales/zh-CN.js`
- Modify: `ruoyi-ui/src/i18n/locales/en-US.js`

**Interfaces:**
- Consumes: none
- Produces: `common.*`, `route.*`, `login.*`, `error.*`, `modal.*`, `toolbar.*`, `pagination.*` keys used by later tasks

- [ ] **Step 1: Define common keys in zh-CN.js**

```js
export default {
  common: {
    search: '搜索',
    reset: '重置',
    add: '新增',
    edit: '修改',
    delete: '删除',
    export: '导出',
    import: '导入',
    submit: '提交',
    cancel: '取消',
    confirm: '确定',
    operation: '操作',
    status: '状态',
    createTime: '创建时间',
    yes: '是',
    no: '否',
    tip: '提示'
  },
  route: {
    dashboard: '首页',
    profile: '个人中心',
    lockScreen: '锁定屏幕'
  },
  login: {
    title: 'xxx管理系统',
    username: '账号',
    usernamePlaceholder: '请输入您的账号',
    password: '密码',
    passwordPlaceholder: '请输入您的密码',
    remember: '记住密码',
    submit: '登 录',
    loading: '登 录 中...',
    usernameRequired: '请输入您的账号',
    passwordRequired: '请输入您的密码',
    captchaRequired: '请输入验证码',
    code: '验证码',
    codePlaceholder: '请输入验证码'
  },
  error: {
    timeout: '系统接口请求超时',
    network: '后端接口连接异常',
    unknown: '系统未知错误',
    unauthorized: '登录状态已过期，您可以继续留在该页面，或者重新登录',
    unauthorizedConfirm: '重新登录',
    downloadError: '下载文件出现错误，请联系管理员！'
  },
  modal: {
    title: '系统提示',
    confirm: '确定',
    cancel: '取消'
  },
  toolbar: {
    hideSearch: '隐藏搜索',
    showSearch: '显示搜索',
    refresh: '刷新',
    column: '显隐列',
    show: '显示',
    hide: '隐藏'
  },
  pagination: {
    total: '共 {total} 条'
  }
}
```

- [ ] **Step 2: Define common keys in en-US.js**

Mirror the Chinese keys with English translations:
```js
export default {
  common: {
    search: 'Search',
    reset: 'Reset',
    add: 'Add',
    edit: 'Edit',
    delete: 'Delete',
    export: 'Export',
    import: 'Import',
    submit: 'Submit',
    cancel: 'Cancel',
    confirm: 'OK',
    operation: 'Operation',
    status: 'Status',
    createTime: 'Create Time',
    yes: 'Yes',
    no: 'No',
    tip: 'Tip'
  },
  route: {
    dashboard: 'Dashboard',
    profile: 'Profile',
    lockScreen: 'Lock Screen'
  },
  login: {
    title: 'xxx Management System',
    username: 'Username',
    usernamePlaceholder: 'Please enter your username',
    password: 'Password',
    passwordPlaceholder: 'Please enter your password',
    remember: 'Remember me',
    submit: 'Sign in',
    loading: 'Signing in...',
    usernameRequired: 'Please enter your username',
    passwordRequired: 'Please enter your password',
    captchaRequired: 'Please enter captcha',
    code: 'Captcha',
    codePlaceholder: 'Please enter captcha'
  },
  error: {
    timeout: 'System interface request timeout',
    network: 'Backend interface connection exception',
    unknown: 'System unknown error',
    unauthorized: 'Login status has expired, you can stay on this page or log in again',
    unauthorizedConfirm: 'Log in again',
    downloadError: 'An error occurred while downloading the file, please contact the administrator!'
  },
  modal: {
    title: 'System Prompt',
    confirm: 'OK',
    cancel: 'Cancel'
  },
  toolbar: {
    hideSearch: 'Hide Search',
    showSearch: 'Show Search',
    refresh: 'Refresh',
    column: 'Column Display',
    show: 'Show',
    hide: 'Hide'
  },
  pagination: {
    total: 'Total {total} items'
  }
}
```

- [ ] **Step 3: Verify no syntax errors**

Run:
```bash
cd ruoyi-ui && npm run build:prod
```

- [ ] **Step 4: Commit**

```bash
git add ruoyi-ui/src/i18n/locales
git commit -m "feat(i18n): add common Chinese and English locale messages"
```

---

## Task 3: Add locale state to Vuex store

**Files:**
- Modify: `ruoyi-ui/src/store/modules/app.js`
- Modify: `ruoyi-ui/src/store/getters.js`

**Interfaces:**
- Consumes: `setLocale` from `src/i18n`
- Produces: `state.app.locale`, `getters.locale`, `actions/setLocale`

- [ ] **Step 1: Modify app.js**

In `ruoyi-ui/src/store/modules/app.js`, update state:
```js
import { setLocale as i18nSetLocale, DEFAULT_LOCALE } from '@/i18n'

const state = {
  sidebar: {
    opened: Cookies.get('sidebarStatus') ? !!+Cookies.get('sidebarStatus') : true,
    withoutAnimation: false
  },
  device: 'desktop',
  size: Cookies.get('size') || 'medium',
  locale: localStorage.getItem('ruoyi-locale') || DEFAULT_LOCALE
}
```

Add mutation:
```js
SET_LOCALE: (state, locale) => {
  state.locale = locale
}
```

Add action:
```js
setLocale({ commit }, locale) {
  i18nSetLocale(locale)
  commit('SET_LOCALE', locale)
}
```

- [ ] **Step 2: Modify getters.js**

Add:
```js
locale: state => state.app.locale,
```

- [ ] **Step 3: Verify build**

Run:
```bash
cd ruoyi-ui && npm run build:prod
```

- [ ] **Step 4: Commit**

```bash
git add ruoyi-ui/src/store/modules/app.js ruoyi-ui/src/store/getters.js
git commit -m "feat(i18n): add locale state to vuex store"
```

---

## Task 4: Create LangSelect component and add it to Navbar

**Files:**
- Create: `ruoyi-ui/src/components/LangSelect/index.vue`
- Modify: `ruoyi-ui/src/layout/components/Navbar.vue`

**Interfaces:**
- Consumes: `state.app.locale`, `dispatch('app/setLocale')`
- Produces: rendered language dropdown, triggers full page reload after switch

- [ ] **Step 1: Create LangSelect component**

`ruoyi-ui/src/components/LangSelect/index.vue`:
```vue
<template>
  <el-dropdown trigger="click" class="international" @command="handleSetLanguage">
    <div>
      <svg-icon icon-class="language" />
    </div>
    <el-dropdown-menu slot="dropdown">
      <el-dropdown-item command="zh-CN" :disabled="language === 'zh-CN'">
        中文
      </el-dropdown-item>
      <el-dropdown-item command="en-US" :disabled="language === 'en-US'">
        English
      </el-dropdown-item>
    </el-dropdown-menu>
  </el-dropdown>
</template>

<script>
export default {
  computed: {
    language() {
      return this.$store.getters.locale
    }
  },
  methods: {
    handleSetLanguage(lang) {
      this.$i18n.locale = lang
      this.$store.dispatch('app/setLocale', lang).then(() => {
        location.reload()
      })
    }
  }
}
</script>
```

Add a `language` icon to `ruoyi-ui/src/assets/icons/` if it does not exist. If missing, use any existing icon temporarily and note it.

- [ ] **Step 2: Add LangSelect to Navbar**

In `ruoyi-ui/src/layout/components/Navbar.vue`, import and place the component near the existing avatar/logout icons:
```js
import LangSelect from '@/components/LangSelect'
```

Register in `components`:
```js
components: {
  ...,
  LangSelect
}
```

Insert the template tag in the right-hand icon area, e.g.:
```vue
<lang-select class="right-menu-item hover-effect" />
```

- [ ] **Step 3: Verify UI**

Run `npm run dev` and confirm a language dropdown appears in the top navbar and can switch languages.

- [ ] **Step 4: Commit**

```bash
git add ruoyi-ui/src/components/LangSelect ruoyi-ui/src/layout/components/Navbar.vue
git commit -m "feat(i18n): add language switch component in navbar"
```

---

## Task 5: Replace global text in utility files

**Files:**
- Modify: `ruoyi-ui/src/utils/request.js`
- Modify: `ruoyi-ui/src/plugins/modal.js`
- Modify: `ruoyi-ui/src/utils/errorCode.js`
- Modify: `ruoyi-ui/src/utils/ruoyi.js`
- Modify: `ruoyi-ui/src/components/RightToolbar/index.vue`

**Interfaces:**
- Consumes: `common.*`, `error.*`, `modal.*`, `toolbar.*` keys from Task 2
- Produces: translated runtime messages and UI labels

- [ ] **Step 1: Update request.js**

Replace the re-login confirmation block:
```js
import i18n from '@/i18n'

MessageBox.confirm(i18n.t('error.unauthorized'), i18n.t('modal.title'), {
  confirmButtonText: i18n.t('error.unauthorizedConfirm'),
  cancelButtonText: i18n.t('modal.cancel'),
  type: 'warning'
})
```

Replace other hardcoded strings:
```js
let message = i18n.t('error.network')
...
message = i18n.t('error.timeout')
...
Message.error(i18n.t('error.downloadError'))
```

- [ ] **Step 2: Update modal.js**

```js
import i18n from '@/i18n'

const defaultTitle = i18n.t('modal.title')
const confirmText = i18n.t('modal.confirm')
const cancelText = i18n.t('modal.cancel')
```

Use these variables in `alert`, `confirm`, `confirmLong` methods.

- [ ] **Step 3: Update errorCode.js**

Replace the Chinese strings with keys and provide a helper:
```js
const codeMap = {
  '401': 'error.code.401',
  '403': 'error.code.403',
  '404': 'error.code.404',
  'default': 'error.code.default'
}

export default function getErrorMsg(code) {
  const key = codeMap[code] || codeMap['default']
  return i18n.t(key)
}
```

Add the corresponding keys to `zh-CN.js` and `en-US.js`:
```js
error: {
  code: {
    '401': '认证失败，无法访问系统资源',
    '403': '当前操作没有权限',
    '404': '访问资源不存在',
    'default': '系统未知错误，请反馈给管理员'
  }
}
```

Update `ruoyi-ui/src/utils/request.js` to use the exported function instead of direct `errorCode[code]`.

- [ ] **Step 4: Update ruoyi.js weekday formatter**

Change:
```js
if (key === 'a') { return ['日', '一', '二', '三', '四', '五', '六'][value] }
```
to use i18n:
```js
if (key === 'a') {
  return i18n.t('date.weekdays')[value]
}
```

Add to locale files:
```js
date: {
  weekdays: ['日', '一', '二', '三', '四', '五', '六']
}
```

```js
date: {
  weekdays: ['Sun', 'Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat']
}
```

- [ ] **Step 5: Update RightToolbar**

Replace hardcoded tooltip contents and transfer titles:
```vue
<el-tooltip :content="showSearch ? $t('toolbar.hideSearch') : $t('toolbar.showSearch')" ...>
<el-tooltip :content="$t('toolbar.refresh')" ...>
<el-tooltip :content="$t('toolbar.column')" ...>
<el-transfer :titles="[$t('toolbar.show'), $t('toolbar.hide')]" ...>
```

- [ ] **Step 6: Verify build**

Run:
```bash
cd ruoyi-ui && npm run build:prod
```

- [ ] **Step 7: Commit**

```bash
git add ruoyi-ui/src/utils/request.js ruoyi-ui/src/plugins/modal.js ruoyi-ui/src/utils/errorCode.js ruoyi-ui/src/utils/ruoyi.js ruoyi-ui/src/components/RightToolbar/index.vue ruoyi-ui/src/i18n/locales
git commit -m "feat(i18n): translate global utility messages and toolbar"
```

---

## Task 6: Translate login page and route titles

**Files:**
- Modify: `ruoyi-ui/src/views/login.vue`
- Modify: `ruoyi-ui/src/router/index.js`
- Modify: `ruoyi-ui/src/i18n/locales/zh-CN.js` and `en-US.js` (add route/login keys)

**Interfaces:**
- Consumes: `login.*`, `route.*` keys
- Produces: translated login UI and document title

- [ ] **Step 1: Update login.vue**

Replace all hardcoded Chinese strings with `$t(...)`:
```vue
<el-input placeholder="$t('login.usernamePlaceholder')" />
<el-input placeholder="$t('login.passwordPlaceholder')" />
<el-checkbox> {{ $t('login.remember') }} </el-checkbox>
<span v-if="!loading">{{ $t('login.submit') }}</span>
<span v-else>{{ $t('login.loading') }}</span>
```

Update validation rules:
```js
loginRules: {
  username: [{ required: true, trigger: 'blur', message: this.$t('login.usernameRequired') }],
  password: [{ required: true, trigger: 'blur', message: this.$t('login.passwordRequired') }]
}
```

Note: if `loginRules` is defined in `data()`, `this.$t` is available. If defined outside, move it into `data()`.

- [ ] **Step 2: Update router/index.js**

Change route meta titles to keys:
```js
meta: { title: 'route.dashboard', icon: 'dashboard', affix: true }
meta: { title: 'route.lockScreen' }
meta: { title: 'route.profile', icon: 'user' }
```

Find where `meta.title` is used to set `document.title` (likely in `src/utils/ruoyi.js` `getPageTitle` or route guards). Update it to:
```js
import i18n from '@/i18n'
const pageName = i18n.t(route.meta.title)
```

- [ ] **Step 3: Add route keys to locales**

In `zh-CN.js`:
```js
route: {
  dashboard: '首页',
  profile: '个人中心',
  lockScreen: '锁定屏幕'
}
```

In `en-US.js`:
```js
route: {
  dashboard: 'Dashboard',
  profile: 'Profile',
  lockScreen: 'Lock Screen'
}
```

- [ ] **Step 4: Verify build**

Run:
```bash
cd ruoyi-ui && npm run build:prod
```

- [ ] **Step 5: Commit**

```bash
git add ruoyi-ui/src/views/login.vue ruoyi-ui/src/router/index.js ruoyi-ui/src/i18n/locales
git commit -m "feat(i18n): translate login page and route titles"
```

---

## Task 7: Translate layout components

**Files:**
- Modify: `ruoyi-ui/src/layout/components/Navbar.vue`
- Modify: `ruoyi-ui/src/layout/components/TagsView/index.vue`
- Modify: `ruoyi-ui/src/layout/components/Sidebar/Item.vue` (if menu titles need translation)
- Modify: `ruoyi-ui/src/i18n/locales/zh-CN.js` and `en-US.js`

**Interfaces:**
- Consumes: `common.*`, `route.*`, `layout.*` keys
- Produces: translated layout UI

- [ ] **Step 1: Add layout keys**

In `zh-CN.js`:
```js
layout: {
  logout: '退出登录',
  profile: '个人中心',
  home: '首页',
  dashboard: '首页'
}
```

In `en-US.js`:
```js
layout: {
  logout: 'Log Out',
  profile: 'Profile',
  home: 'Home',
  dashboard: 'Dashboard'
}
```

- [ ] **Step 2: Update Navbar.vue**

Replace hardcoded dropdown items like "个人中心" / "退出登录" with `$t('layout.profile')` / `$t('layout.logout')`.

- [ ] **Step 3: Update TagsView**

Replace hardcoded "首页" / "关闭" / "关闭其它" / "关闭全部" with keys. Add to locale files:
```js
tagsView: {
  close: '关闭',
  closeOthers: '关闭其它',
  closeAll: '关闭全部',
  refresh: '刷新'
}
```

- [ ] **Step 4: Update Sidebar menu title rendering**

Dynamic menus from backend are Chinese strings. For the short term, add a mapping in locale files under `menu.*` keyed by route name, and in `Sidebar/Item.vue` use `menu[routeName]` if available, otherwise fallback to the title from the menu item.

Example:
```vue
<template>
  <span>{{ menuTitle }}</span>
</template>
<script>
export default {
  computed: {
    menuTitle() {
      const key = `menu.${this.item.name}`
      const translated = this.$te(key) ? this.$t(key) : null
      return translated || this.item.meta.title || this.item.title
    }
  }
}
</script>
```

Add keys:
```js
menu: {
  system: '系统管理',
  'system-user': '用户管理',
  'system-role': '角色管理',
  monitor: '系统监控',
  tool: '系统工具',
  bpm: '流程管理'
}
```

In en-US.js mirror these.

- [ ] **Step 5: Verify build**

Run:
```bash
cd ruoyi-ui && npm run build:prod
```

- [ ] **Step 6: Commit**

```bash
git add ruoyi-ui/src/layout ruoyi-ui/src/i18n/locales
git commit -m "feat(i18n): translate layout components and sidebar menu titles"
```

---

## Task 8: Translate system/user module as example

**Files:**
- Modify: `ruoyi-ui/src/views/system/user/index.vue`
- Modify: `ruoyi-ui/src/views/system/user/authRole.vue`
- Modify: `ruoyi-ui/src/i18n/locales/zh-CN.js` and `en-US.js`

**Interfaces:**
- Consumes: `common.*`, `user.*` keys
- Produces: translated user management UI

- [ ] **Step 1: Add user module keys**

In `zh-CN.js`:
```js
user: {
  title: '用户管理',
  add: '新增用户',
  edit: '修改用户',
  deleteConfirm: '是否确认删除用户编号为"{userIds}"的数据项？',
  importTitle: '用户导入',
  form: {
    userName: '用户名称',
    userNamePlaceholder: '请输入用户名称',
    nickName: '用户昵称',
    phonenumber: '手机号码',
    status: '用户状态',
    deptId: '归属部门',
    roleIds: '角色'
  },
  table: {
    userName: '用户名称',
    nickName: '用户昵称',
    deptName: '部门',
    phonenumber: '手机号码',
    status: '状态',
    createTime: '创建时间'
  }
}
```

In `en-US.js`:
```js
user: {
  title: 'User Management',
  add: 'Add User',
  edit: 'Edit User',
  deleteConfirm: 'Are you sure to delete user(s) with ID "{userIds}"?',
  importTitle: 'User Import',
  form: {
    userName: 'User Name',
    userNamePlaceholder: 'Please enter user name',
    nickName: 'Nickname',
    phonenumber: 'Phone Number',
    status: 'Status',
    deptId: 'Department',
    roleIds: 'Roles'
  },
  table: {
    userName: 'User Name',
    nickName: 'Nickname',
    deptName: 'Department',
    phonenumber: 'Phone Number',
    status: 'Status',
    createTime: 'Create Time'
  }
}
```

- [ ] **Step 2: Update user/index.vue**

Replace all user-facing Chinese strings with `$t('user.xxx')` or `$t('common.xxx')`. Examples:
```vue
<el-form-item label="用户名称" prop="userName">
```
to:
```vue
<el-form-item :label="$t('user.form.userName')" prop="userName">
```

Buttons:
```vue
<el-button type="primary" icon="el-icon-search" size="mini" @click="handleQuery">{{ $t('common.search') }}</el-button>
```

Table columns:
```vue
<el-table-column label="用户名称" ... />
```
to:
```vue
<el-table-column :label="$t('user.table.userName')" ... />
```

- [ ] **Step 3: Update user/authRole.vue**

Similarly replace Chinese labels in the role assignment dialog.

- [ ] **Step 4: Verify build**

Run:
```bash
cd ruoyi-ui && npm run build:prod
```

- [ ] **Step 5: Commit**

```bash
git add ruoyi-ui/src/views/system/user ruoyi-ui/src/i18n/locales
git commit -m "feat(i18n): translate system/user module as example"
```

---

## Task 9: Backend language preference support

**Files:**
- Modify: `ruoyi-system/src/main/java/com/ruoyi/system/domain/SysUser.java`
- Modify: `ruoyi-system/src/main/resources/mapper/system/SysUserMapper.xml`
- Modify: `ruoyi-system/src/main/java/com/ruoyi/system/service/impl/SysUserServiceImpl.java`
- Modify: `ruoyi-admin/src/main/java/com/ruoyi/web/controller/system/SysProfileController.java`
- Modify: `ruoyi-framework/src/main/java/com/ruoyi/framework/web/service/SysLoginService.java`
- Modify: `ruoyi-admin/src/main/java/com/ruoyi/web/controller/system/SysLoginController.java`
- SQL migration: add `lang` column to `sys_user`

**Interfaces:**
- Consumes: none
- Produces: user.lang field persisted and returned on login/getInfo

- [ ] **Step 1: Add lang column to sys_user table**

Create `sql/i18n_user_lang.sql`:
```sql
ALTER TABLE sys_user ADD COLUMN lang VARCHAR(10) DEFAULT 'zh-CN' COMMENT '语言偏好' AFTER status;
```

Run it against the development database or deliver it for manual execution.

- [ ] **Step 2: Update SysUser domain**

Add field, getter, setter:
```java
private String lang;
```

- [ ] **Step 3: Update UserMapper.xml**

Add `lang` to resultMap, insert, and update statements.

- [ ] **Step 4: Update SysUserServiceImpl**

When inserting a new user, default `lang` to `zh-CN` if null. When updating profile, allow `lang` to be updated.

- [ ] **Step 5: Update SysLoginService.login**

After successful authentication, include the user's `lang` in the login response or ensure it is available via `getInfo`.

- [ ] **Step 6: Update SysLoginController.getInfo**

Add `lang` to the returned user info map.

- [ ] **Step 7: Update SysProfileController.updateProfile**

Accept `lang` in the request body and update the current user's preference.

- [ ] **Step 8: Build backend**

Run:
```bash
export JAVA_HOME=/d/tools/jdk17 && export PATH=$JAVA_HOME/bin:$PATH
mvn -pl ruoyi-admin -am clean package -DskipTests
```

- [ ] **Step 9: Commit**

```bash
git add ruoyi-system ruoyi-admin ruoyi-framework sql/i18n_user_lang.sql
git commit -m "feat(i18n): backend user language preference support"
```

---

## Task 10: Frontend sync user lang from backend and build verification

**Files:**
- Modify: `ruoyi-ui/src/store/modules/user.js`
- Modify: `ruoyi-ui/src/views/login.vue`
- Modify: `ruoyi-ui/src/App.vue` (optional, for initial lang sync)

**Interfaces:**
- Consumes: backend `lang` field from login/getInfo
- Produces: frontend locale synchronized with user profile

- [ ] **Step 1: Sync lang after login**

In `ruoyi-ui/src/store/modules/user.js`, in the login action success handler:
```js
import { setLocale } from '@/i18n'

login({ commit }, userInfo) {
  return new Promise((resolve, reject) => {
    login(username, password, code, uuid).then(res => {
      setToken(res.token)
      commit('SET_TOKEN', res.token)
      if (res.lang) {
        setLocale(res.lang)
        commit('SET_LOCALE', res.lang, { root: true })
      }
      resolve()
    }).catch(error => {
      reject(error)
    })
  })
}
```

- [ ] **Step 2: Sync lang in getInfo**

In the `getInfo` action:
```js
getInfo({ commit, state }) {
  return new Promise((resolve, reject) => {
    getInfo().then(res => {
      const user = res.user
      if (user.lang) {
        setLocale(user.lang)
        commit('SET_LOCALE', user.lang, { root: true })
      }
      ...
    })
  })
}
```

- [ ] **Step 3: Add updateLang action**

```js
updateLang({ commit }, lang) {
  return updateProfile({ lang }).then(() => {
    setLocale(lang)
    commit('SET_LOCALE', lang, { root: true })
  })
}
```

- [ ] **Step 4: Update LangSelect to persist to backend**

Modify `LangSelect` to call `this.$store.dispatch('user/updateLang', lang)` when a logged-in user switches language, falling back to local-only for the login page.

- [ ] **Step 5: Final build verification**

Run:
```bash
cd ruoyi-ui && npm run build:prod
```

Run backend build:
```bash
export JAVA_HOME=/d/tools/jdk17 && export PATH=$JAVA_HOME/bin:$PATH
mvn -pl ruoyi-admin -am clean package -DskipTests
```

- [ ] **Step 6: Commit**

```bash
git add ruoyi-ui/src/store ruoyi-ui/src/components/LangSelect ruoyi-ui/src/App.vue
git commit -m "feat(i18n): sync language preference with backend user profile"
```

---

## Task 11: Documentation and migration script

**Files:**
- Create: `docs/i18n-migration-guide.md`
- Create: `ruoyi-ui/scripts/find-untranslated.js`

**Interfaces:**
- Consumes: source files
- Produces: migration guide and scan report

- [ ] **Step 1: Write migration guide**

`docs/i18n-migration-guide.md` should cover:
- Key naming convention.
- How to add a new language.
- How to translate a new module.
- How to update backend messages.

- [ ] **Step 2: Create scan script**

`ruoyi-ui/scripts/find-untranslated.js` scans `.vue` and `.js` files for common Chinese characters outside `$t(...)` and prints a report.

Example:
```js
const fs = require('fs')
const path = require('path')
const glob = require('glob')

const files = glob.sync('src/**/*.{vue,js}', { cwd: path.join(__dirname, '..') })
files.forEach(file => {
  const content = fs.readFileSync(path.join(__dirname, '..', file), 'utf-8')
  const lines = content.split('\n')
  lines.forEach((line, idx) => {
    if (/[\u4e00-\u9fa5]/.test(line) && !line.includes('$t(')) {
      console.log(`${file}:${idx + 1}: ${line.trim()}`)
    }
  })
})
```

- [ ] **Step 3: Commit**

```bash
git add docs/i18n-migration-guide.md ruoyi-ui/scripts/find-untranslated.js
git commit -m "docs(i18n): add migration guide and untranslated text scanner"
```

---

## Self-Review

### Spec coverage

| Spec requirement | Covered by task |
|---|---|
| vue-i18n@8 基础设施 | Task 1 |
| 中/英文 locale 文件 | Task 2 |
| 语言切换组件 | Task 4 |
| Element UI 语言包联动 | Task 1 |
| 公共文案提取 | Task 2, 5, 6, 7 |
| system/user 示例模块 | Task 8 |
| 后端用户语言偏好 | Task 9 |
| 前后端语言同步 | Task 10 |
| 扩展更多语言预留接口 | Task 2, 11 |
| build 通过 | Every task Step N |

### Placeholder scan

No TBD/TODO/"implement later"/"similar to" placeholders. Each step provides exact file paths, code snippets, and commands.

### Type consistency

- `setLocale` signature consistent across `src/i18n/index.js`, store action, and user actions.
- Locale codes consistently `zh-CN` / `en-US`.
- Vuex mutation name `SET_LOCALE` consistent.

---

## Execution Handoff

Plan complete and saved to `docs/superpowers/plans/2026-06-28-i18n-plan.md`.

**Two execution options:**

1. **Subagent-Driven (recommended)** — dispatch a fresh subagent per task, review between tasks, fast iteration.
2. **Inline Execution** — execute tasks in this session using `executing-plans`, batch execution with checkpoints.

Choose an approach to proceed.
